---
import { sanityClient } from '../../../lib/sanity'
import { navSettingsQuery } from './nav.query'
import { NavSettingsSchema } from './nav.schema'
import NavBarView from './NavBarView.astro'

const nav = await NavSettingsSchema.parseAsync(await sanityClient.fetch(navSettingsQuery))
---

<NavBarView nav={nav} />
