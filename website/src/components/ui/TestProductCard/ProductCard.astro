---
import type { ProductCard } from '../../../lib/schemas/cards/productCard';
import CTAButtons from '../ctaButton/render/CTAButtons.astro';
import ProductCardView from './ProductCardView.astro';

const { ctaButton, ...props } = Astro.props as ProductCard;
---

<ProductCardView {...props}>
  <CTAButtons slot="cta" cta={ctaButton ?? []} />
</ProductCardView>
